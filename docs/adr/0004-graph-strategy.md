# ADR 0004: Graph as an optional index

Status: accepted.

Use relationship rows and temporal links in the relational model; defer a graph database until an ablation proves a meaningful retrieval or continuation gain. Graphiti and Cognee show graph value, but also show the driver, schema, and deployment cost. Rejected: graph-first authority for V1.

